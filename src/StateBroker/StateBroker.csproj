<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <PublishAot>true</PublishAot>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="StateBroker.Tests" />
    <InternalsVisibleTo Include="StateBroker.TestClient" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="appsettings.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <PropertyGroup Condition="'$(EnableRaft)' == 'true'">
    <DefineConstants>$(DefineConstants);ENABLE_RAFT</DefineConstants>
  </PropertyGroup>

  <ItemGroup Condition="'$(EnableRaft)' == 'true'">
    <PackageReference Include="DotNext.Net.Cluster" Version="5.*" />
  </ItemGroup>
</Project>
